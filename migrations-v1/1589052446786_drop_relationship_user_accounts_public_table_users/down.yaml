- args:
    name: user_accounts
    table:
      name: users
      schema: public
    using:
      foreign_key_constraint_on:
        column: user_id
        table:
          name: user_accounts
          schema: auth
  type: create_array_relationship
