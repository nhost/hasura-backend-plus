name: Check Pull Request
on:
  pull_request:
    branches: [v2]
    paths-ignore:
      - 'docs/**'
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run Jest tests
        run: ./run_tests.sh
